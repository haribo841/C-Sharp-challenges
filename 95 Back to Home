//You have started your journey from home.
//Given a string of directions (N=North, W=West, S=South, E=East), you will walk for one minute in each direction.
//Determine whether a set of directions will lead you back to the starting position or not.
using System;
using NUnit.Framework;

[TestFixture]
public class Tests
{
  [Test]
	[TestCase("NNNN", Result=false)]
	[TestCase("NENESSWW", Result=true)]
	[TestCase("NEESSW", Result=false)]
	[TestCase("EEWE", Result=false)]
	[TestCase("NNSSEEEWWWEW", Result=true)]

	public static bool BackToHome(string direction)
	{
		Console.WriteLine($"Input: {direction}");
		return Program.BackToHome(direction);
	}
}
public class Program 
{
    public static bool BackToHome(string d)
    {
			
    }
}
