//Write a function that takes two numbers and returns if they should be added, subtracted, multiplied or divided to get 24.
//If none of the operations can give 24, return "none".
using NUnit.Framework;

[TestFixture]
public class Tests
{
	[Test]
	[TestCase(12, 12, Result="added")]
	[TestCase(100, 76, Result="subtracted")]
	[TestCase(6, 4, Result="multiplied")]
	[TestCase(528, 22, Result="divided")]
	[TestCase(10, 12, Result="none")]
	public static string FixedTest(int num1, int num2) =>
		Program.Operation(num1, num2);
}
public class Program
{
	public static string Operation(int num1, int num2)
	{
		
	}
}
