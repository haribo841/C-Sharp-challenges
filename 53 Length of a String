//Write a function that returns the length of a string.
//Make your function recursive.
using System;
using NUnit.Framework;

[TestFixture]
public class Tests {
	[Test]
	[TestCase("apple", Result=5)]
	[TestCase("make", Result=4)]
	[TestCase("a", Result=1)]
	[TestCase("", Result=0)]
	public static int FixedTest(string a) {
		Console.WriteLine($"Input: {a}");
		return Program.Length(a);
	}
}
public class Program
{
	public static int Length(string str) =>
		
}
